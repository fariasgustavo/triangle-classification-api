region: us-east-1

cors:
  ALLOW_ORIGIN: '*'
  ALLOW_HEADERS: 'GET, POST, PUT, PATCH, DELETE, OPTIONS'
  ALLOW_METHODS: 'Content-Type, Authorization, X-SessionID'

environment:
  TOKEN_SECRET_ID: toroiac-secrets-webapi-token-hml

database:
  triangleHistory: toro-user-position-api-dev
